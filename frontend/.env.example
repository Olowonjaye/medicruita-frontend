# Example Vite environment file for the frontend
# Copy this to .env and replace the URL with your backend
# Preferred variable name for the frontend API base
VITE_API_URL=https://medicruita-backend.onrender.com
# (fallback) older projects may still use VITE_API_BASE_URL
VITE_API_BASE_URL=https://medicruita-backend.onrender.com
